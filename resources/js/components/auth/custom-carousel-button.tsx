import React from 'react'

function custom-carousel-button() {
    const { orientation, scrollPrev, canScrollPrev } = useCarousel()
  return (
    <div>custom-carousel-button</div>
  )
}

export default custom-carousel-button